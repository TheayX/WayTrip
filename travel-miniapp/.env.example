## WayTrip 小程序端环境变量模板
## 使用方式：
## 1. 复制当前文件为 .env.local
## 2. 按当前联调场景修改源站地址
## 3. .env.local 仅供个人机器使用，不提交到 Git
##
## 设计约定：
## 1. 协议由环境变量决定，代码不再自动把 HTTP 改成 HTTPS。
## 2. 接口与图片资源默认同源，便于本地 HTTP、Nginx HTTPS 反代、真机局域网联调三种场景直接切换。
## 3. 只有资源与接口不在同一个源站时，才额外配置 VITE_RESOURCE_ORIGIN。

## 小程序接口源站地址
## 常见场景：
## - 日常开发： http://localhost:8080
## - 本机 HTTPS 反代： https://localhost:8443
## - 真机调试： http://你的局域网IP:8080
VITE_API_ORIGIN=http://localhost:8080

## 小程序资源源站地址
## 默认留空，图片等资源会跟随 VITE_API_ORIGIN。
## 只有资源与接口不在同一个源站时才需要配置，例如 CDN 或单独的静态资源域名。
# VITE_RESOURCE_ORIGIN=
