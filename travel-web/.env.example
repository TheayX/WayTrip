## WayTrip Web 用户端环境变量模板
## 使用方式：
## 1. 复制当前文件为 .env.local
## 2. 按你的本地联调方式修改下面的值
## 3. .env.local 仅供个人机器使用，不提交到 Git

## 本地开发时 /api 和 /uploads 的代理目标
## 默认直接代理到本地 Spring Boot 服务。
## 如果你希望统一走本机 HTTPS 反代，可改为 https://localhost:8443。
VITE_DEV_PROXY_TARGET=http://localhost:8080

## 开发服务器监听地址
## 默认通常留空即可；只有在 Nginx 反代或特殊网络环境下再改成 127.0.0.1。
VITE_DEV_HOST=

## 允许访问当前 Vite 开发服务器的额外 Host 列表，多个值用英文逗号分隔。
## 默认留空；仅在 ngrok 或其他外部域名接入时填写。
VITE_DEV_ALLOWED_HOSTS=
